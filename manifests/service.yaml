apiVersion: v1
kind: Service
metadata:
  name: contoso-air-rwood
  namespace: dev
  labels:
    app.kubernetes.io/name: contoso-air-rwood
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: contoso-air-rwood
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000